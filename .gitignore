# Datos descargados (no commitear los ficheros grandes de la PLACSP)
data/
*.zip
*.atom

# pliegos/<id_evl>/: anexos originales de cada expediente para seguimiento
# manual. Los PDF pesan y son descargables de nuevo desde PLACSP -> no van a
# git. Los .md (notas, resumenes) si van, son los que aportan valor versionado.
pliegos/**/*.pdf

# Python
__pycache__/
*.pyc
.venv/
venv/

# Secretos — por si acaso, nunca commitear
.env

# Sistema
.DS_Store
Thumbs.db
